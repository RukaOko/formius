const CheckStructure = (configForm) => {
    var errors = [];

    if(errors.length === 0)
    {
        return false;
    }
    else
    {
        return errors;
    }
}
export default CheckStructure;